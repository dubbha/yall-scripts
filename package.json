{
  "name": "yarn-all",
  "version": "1.0.0",
  "description": "Run several npm scripts in parallel or sequence forwarding the options in a yarn-friendly manner",
  "bin": {
    "yarn-all": "bin/yarn-all.js"
  },
  "main": "lib/index.js",
  "engines": {
    "node": ">= 10",
    "yarn": ">= 1"
  },
  "scripts": {
    "lint:bin": "eslint ./bin",
    "lint:lib": "eslint ./lib",
    "lint": "node ./bin/yarn-all.js lint:lib lint:bin"
  },
  "author": "Alex Dubovyk",
  "license": "MIT",
  "dependencies": {
    "cross-spawn": "^7.0.2"
  },
  "devDependencies": {
    "eslint": "^6.8.0"
  }
}
